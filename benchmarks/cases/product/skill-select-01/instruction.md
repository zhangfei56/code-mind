# skill-select-01

Regression coverage for automatic skill matching lives in:

- `tests/unit/capability-selector.test.ts`
- `tests/unit/skill-policy.test.ts`

This case id is reserved for future agent-level eval when a mock model is wired.
